[project]
name = "extraction-racer-revolution"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.22",
    "pillow>=9.0",
    "pygltflib>=1.15",
]
